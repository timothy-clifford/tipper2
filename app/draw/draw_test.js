'use strict';

describe('tipper2.draw module', function() {

  beforeEach(module('tipper2.draw'));

  describe('draw controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('DrawCtrl');
      expect(drawCtrl).toBeDefined();
    }));

  });
});